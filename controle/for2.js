const notas = [0,4,6,9,2,3,1,8]

for (let i in notas){
    console.log(i, notas[i]);
}

const pessoa = {
    nome: 'Ana', 
    sobrenome: 'peixoto',
    idade: 25,
    peso:64
}

for (let laco in pessoa){
    console.log(laco, pessoa[laco]);
}